{
  "name": "market-data",
  "version": "0.1.0",
  "private": true,
  "description": "thinkorswim web market-data harvester (CDP passive capture + state reconstruction)",
  "type": "commonjs",
  "scripts": {
    "capture": "node scripts/cdp-capture.js",
    "daemon": "node src/daemon.js",
    "replay": "node src/replay.js"
  },
  "dependencies": {
    "playwright": "^1.61.1",
    "ws": "^8.18.0"
  }
}
